import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.personale.lamultipla',
  appName: 'La Multipla',
  webDir: 'out',
  android: { backgroundColor: '#061e1a' },
};

export default config;
